import { Copyable } from '@qratch/copyable'

/**
 * Shapeable interface.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Shapeable extends Copyable<Shapeable> {}
