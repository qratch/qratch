/**
 * RGBA type.
 */
export type RGBA = {
  /**
   * red(`0.0 ~ 1.0`).
   */
  r: number

  /**
   * green(`0.0 ~ 1.0`).
   */
  g: number

  /**
   * alpha(`0.0 ~ 1.0`).
   */
  b: number

  /**
   * alpha(`0.0 ~ 1.0`).
   */
  a: number
}
